import { createStore } from 'vuex'
import auth from "@/store/auth";

export default createStore({
  state: {
    skillList: []
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth
  }
})
